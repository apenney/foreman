# Base foreman smart proxy container

This is a base foreman smart proxy container, useful as a base for extending with other services and plugins.

```
$ docker run --name foreman-proxy -d foreman-proxy
```
